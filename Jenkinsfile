pipeline {
    agent any
    stages {
        stage('Hello') {
            steps {
                echo 'Hello World'
                echo 'Build number from jenkins is ${currentBuild.number}'
            }
        }
    }
}
